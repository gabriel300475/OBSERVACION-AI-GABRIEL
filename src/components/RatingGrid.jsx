import { DIMENSIONES, NIVELES } from '../models'
export default function RatingGrid({ratings,onRate}) { return <section className="panel"><div className="section-heading"><div><span className="kicker">04 / lectura pedagógica</span><h2>Valoración por dimensión</h2></div><span className="scale-hint">1 inicial · 4 destacado</span></div><div className="ratings">{DIMENSIONES.map(item=><div className="rating-row" key={item.key}><div className="rating-copy"><b>{item.label}</b><p>{item.desc}</p></div><div className="rating-buttons">{NIVELES.map(level=><button type="button" className={ratings[item.key]===level.v?'active':''} onClick={()=>onRate(item.key,level.v)} title={level.label} aria-label={`${item.label}: ${level.label}`} key={level.v}>{level.v}</button>)}</div></div>)}</div></section> }
